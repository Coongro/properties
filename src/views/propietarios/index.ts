/**
 * Propietarios — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `propietarios.view.ts`,
 * datos en `use-propietarios.ts`, lógica custom en `handlers.ts`.
 */
export { PropietariosView } from './propietarios.view.js';
