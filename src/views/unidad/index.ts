/**
 * Unidad — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `unidad.view.ts`,
 * datos en `use-unidad.ts`, lógica custom en `handlers.ts`.
 */
export { UnidadView } from './unidad.view.js';
