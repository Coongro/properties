/**
 * Certificado — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `certificado.view.ts`,
 * datos en `use-certificado.ts`, lógica custom en `handlers.ts`.
 */
export { CertificadoView } from './certificado.view.js';
