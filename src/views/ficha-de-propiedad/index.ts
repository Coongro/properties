/**
 * Ficha de propiedad — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `ficha-de-propiedad.view.ts`,
 * datos en `use-ficha-de-propiedad.ts`, lógica custom en `handlers.ts`.
 */
export { FichaDePropiedadView } from './ficha-de-propiedad.view.js';
