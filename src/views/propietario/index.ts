/**
 * Propietario — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `propietario.view.ts`,
 * datos en `use-propietario.ts`, lógica custom en `handlers.ts`.
 */
export { PropietarioView } from './propietario.view.js';
