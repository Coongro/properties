/**
 * Propiedades — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `propiedades.view.ts`,
 * datos en `use-propiedades.ts`, lógica custom en `handlers.ts`.
 */
export { PropiedadesView } from './propiedades.view.js';
