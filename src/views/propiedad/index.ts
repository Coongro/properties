/**
 * Propiedad — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `propiedad.view.ts`,
 * datos en `use-propiedad.ts`, lógica custom en `handlers.ts`.
 */
export { PropiedadView } from './propiedad.view.js';
