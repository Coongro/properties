/**
 * Ficha de unidad — entry de la vista (lo referencia el coongro.manifest.json).
 * Regenerable; no agregar código acá — composición en `ficha-de-unidad.view.ts`,
 * datos en `use-ficha-de-unidad.ts`, lógica custom en `handlers.ts`.
 */
export { FichaDeUnidadView } from './ficha-de-unidad.view.js';
